name: 🧪 Build, Lint & Test

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🐍 Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Set PYTHONPATH
        run: echo "PYTHONPATH=$(pwd)" >> $GITHUB_ENV

      - name: 📦 Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install flake8 pytest

      - name: 🧼 Lint with flake8
        run: |
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=88 --statistics

      - name: 🧪 Run unit tests with pytest
        run: |
          pytest tests/

      - name: 🐳 Build Docker image (CPU)
        run: |
          docker build --build-arg BASE_IMAGE=python:3.10-slim -t myapp:cpu .

      # Optional: GPU build (requires self-hosted runner)
      # - name: 🐳 Build Docker image (GPU)
      #   run: |
      #     docker build --build-arg BASE_IMAGE=pytorch/pytorch:2.1.0-cuda11.8-cudnn8-runtime -t myapp:gpu .
